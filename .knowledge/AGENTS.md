# AGENTS.md — SingChorus 变更守则

1. **改码必改文档**：每次新增、修改代码，必须同步更新 `.knowledge/` 中相关文档（视图 / ADR / 子系统文档），文档与代码同一提交。
2. **变更先评估影响**：新增、修改功能前，必须评估其对整体架构和其他功能的影响，并在提交说明或 ADR 中留痕。
3. **守准则、记决策**：遵守项目原有准则与既有决策（`architecture_views/` 与 `subsystems/` 下的 adrs、guidelines）；重大的决策、准则必须以 ADR / principles 形式记录归档。
